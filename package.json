{
  "name": "edc-lib",
  "version": "0.2.0",
  "description": "TypeScript client for the Eclipse Dataspace Connector (EDC) REST APIs, generated from the official OpenAPI specs with @hey-api/openapi-ts.",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./build/*/index.d.ts",
      "import": "./build/*/index.js"
    }
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "generate": "openapi-ts",
    "compile": "tsc",
    "build": "npm run generate && npm run compile",
    "clean": "rm -rf build",
    "prepare": "npm run compile"
  },
  "author": "Michel Otto",
  "license": "Apache-2.0",
  "devDependencies": {
    "@hey-api/openapi-ts": "0.97.3",
    "typescript": "^5.9.3"
  }
}
